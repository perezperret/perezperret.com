---
layout: default
---

<div class="min-h-screen px-4 py-32 sm:px-0">
  <div class="container mx-auto">
    <div class="mb-20 max-w-2xl">
      <h1 class="text-5xl sm:text-6xl heading leading-none mb-4">{{ page.title }}</h1>
      <p class="max-w-lg text-2xl text-gray-900">{{ page.sub_title }}</p>
    </div>

    {{ content }}
  </div>
</div>

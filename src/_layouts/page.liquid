---
layout: default
---

<div class="min-h-screen bg-white pt-16 pb-40">
  <div class="container mx-auto">
    <div class="max-w-2xl mx-auto">
      <div class="mb-16 max-w-lg">
        <h1 class="text-5xl font-bold text-gray-900">{{ page.title }}</h1>
        <p class="leading-8 text-lg text-gray-800 font-sans my-4">{{ page.sub_title }}</p>
      </div>
      {{ content }}
    </div>
  </div>
</div>

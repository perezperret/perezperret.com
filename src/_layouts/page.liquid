---
layout: default
---

<div class="min-h-screen bg-white py-32">
  <div class="container mx-auto">
    <div class="max-w-2xl">
      <div class="mb-32 max-w-lg">
        <h1 class="text-6xl font-bold text-gray-900">{{ page.title }}</h1>
        <p class="leading-8 text-lg text-gray-800 font-sans mb-4">{{ page.sub_title }}</p>
      </div>
      {{ content }}
    </div>
  </div>
</div>

---
layout: page
title: Work experience
sub_title: I have worked for a few startups and small companies all over the world
permalink: /work
---

<% data = site.data[:work] %>

<div class="mx-auto container">
  <% data[:content].each.with_index do |job, i| %>
    <div class="flex -mx-4 items-stretch <%= (i % 2).positive? ? 'md:flex-row-reverse' : '' %>">
      <div class="px-4 w-full md:w-1/2">
        <div class="md:hidden h-8 flex items-center text-gray-700">
          <%= job[:date] %>
        </div>

        <div class="mb-16 md:mt-16">
          <h3 class="text-2xl heading"><%= job[:title] %></h3>
          <h4 class="font-bold text-gray-700 mb-4"><%= job[:sub_title] %></h4>

          <div class="content">
            <p class="text-base"><%= job[:description] %></p>
          </div>
        </div>
      </div>

      <div class="flex flex-col items-center order-first md:order-none ml-4 md:ml-0">
        <div class="border-r-8 border-blue-500 flex-1 h-full -mb-2"></div>
        <div class="h-8 w-8 order-first md:order-none rounded-full border-solid border-8 border-blue-500"></div>
        <div class="border-r-8 border-blue-500 flex-1 h-full -mt-2"></div>
      </div>

      <div class="hidden px-4 w-1/2 md:flex items-center text-xl text-gray-700 <%= (i % 2).positive? ? 'justify-end' : '' %>">
        <%= job[:date] %>
      </div>
    </div>
  <% end %>
</div>

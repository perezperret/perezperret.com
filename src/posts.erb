---
layout: page
title: Santiago's Blog
sub_title: My thoughts and advice about life, personal finance and software development.
permalink: /blog
---

<form action="https://formkeep.com/f/2196b304bda9" class="mb-4" method="POST" target="_blank">
  <div class="max-w-xl">
    <label for="contact_email" class="block mb-2">Leave your email to receive updates</label>

    <div class="flex flex-wrap md:flex-no-wrap mb-4">
      <input name="subscribe" type="hidden" value="1">
      <input name="blog" type="hidden" value="1">
      <input id="contact_email" name="email" type="text" class="p-4 bg-gray-200 rounded-lg w-full mb-4 md:mb-0 md:mr-4">

      <button class="w-full md:w-auto bg-green-400 rounded-lg py-4 px-6 font-bold md:text-xl text-white">Submit</button>
    </div>
  </div>
</form>

<div class="max-w-2xl pt-16">
  <ul>
    <% site.posts.reverse.each do |post| %>
      <li>
        <a class="block mb-20" href="<%= post.url %>">
          <div class="mb-4">
            <h2 class="text-3xl heading heading--link"><%= post[:title] %></h2>
          </div>

          <div class="content">
            <p><%= post[:excerpt] %></p>
          </div>

          <div class="flex items-center justify-between">
            <div class="flex">
              <% post[:categories].each do |category| %>
                <div class="font-sans text-blue-500 mr-4 uppercase font-bold text-xs"><%= category %></div>
              <% end %>
            </div>

            <div class="font-sans text-gray-600"><%= date_to_string post[:date], 'ordinal', 'US' %></div>
          </div>
        </a>
      </li>
    <% end %>
  </ul>
</div>
